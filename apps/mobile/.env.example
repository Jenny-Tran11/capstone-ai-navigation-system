# Copy to .env (do not commit .env).
# Full URL to API stage root, e.g. https://xxxxx.execute-api.ap-southeast-2.amazonaws.com/prod
EXPO_PUBLIC_DETECT_API_URL=
# API Gateway usage plan key (x-api-key)
EXPO_PUBLIC_DETECT_API_KEY=
