# PyTorch CPU wheels (must be first: extra-index applies to following lines)
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# Pin NumPy/SciPy to releases with prebuilt manylinux wheels (Lambda image has no compiler).
numpy>=2.1.0,<2.3
scipy>=1.11.0,<1.15.0
# matplotlib 3.10+ can pull contourpy versions that only ship sdist on this platform.
matplotlib>=3.3.0,<3.9.0
contourpy>=1.0.0,<1.3.0

ultralytics
opencv-python-headless
boto3
