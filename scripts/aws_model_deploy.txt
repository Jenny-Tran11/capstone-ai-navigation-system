# Deploy detection Lambda + API (CDK)
# Prereqs: Docker Desktop RUNNING, AWS CLI configured, model at apps\aws\lambda\detect\yolo12n.pt

cd D:\Capstone\new-repo\capstone-ai-navigation-system\apps\aws

# One-time per account+region (skip if already bootstrapped):
# npx aws-cdk@latest bootstrap aws://332144430542/ap-southeast-2 --context account=332144430542 --context region=ap-southeast-2

npx aws-cdk@latest deploy BlindNavDetectionStack --require-approval never --context account=332144430542 --context region=ap-southeast-2

# --- If deploy fails with Docker I/O / containerd / meta.db errors ---
# 1) Quit Docker Desktop fully, then reopen. Wait until it says "Engine running".
# 2) PowerShell: wsl --shutdown   (then start Docker again)
# 3) Free disk space on C: (Docker VM disk); in Docker Desktop: Settings -> Resources -> Disk image size
# 4) Still broken: Docker Desktop -> Troubleshoot -> Clean / Purge data (last resort; removes local images)
# 5) Sanity check before redeploy:
#    docker run --rm hello-world
